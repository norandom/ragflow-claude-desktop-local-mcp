[project]
name = "ci"
version = "0.0.0"
description = "Dagger CI module for ragflow-claude-mcp"
requires-python = ">=3.10"
dependencies = [
    "dagger-io>=0.18.10",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/ci"]
